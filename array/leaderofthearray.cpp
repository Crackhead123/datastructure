//In this program we have to print those numbers in the array which are greater the all the number to the right of them.

#include<bits/stdc++.h>
using namespace std;

void leader(int a[],int n)
{
    int curr_leader=a[0];
    for(int i=1;i<n;i++)
    {
        
    }
}


int main()
{
    int a[]={7,10,4,10,6,5,2};
    int n=7;
    leader(a,n);
    return 0;
}